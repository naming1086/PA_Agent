# 提示词装配与两阶段调用流程

> 记录时间：2026-09-24
> 对应代码状态：回滚后的原版装配顺序（`prompt_assembler.py` 未经缓存优化改动）
> 目的：说明 `prompt_engineering/` 里的文件如何被组装成两次 LLM 请求，以及 KV 缓存命中（`hit`）的成因与优化方向。

---

## 1. 总览

一次完整分析 = **两次 LLM 调用**，由 `pa_agent/orchestrator/two_stage.py` 驱动：

| | 第一次（Stage 1） | 第二次（Stage 2） |
|---|---|---|
| 目的 | 诊断市场处于周期频谱哪一处 | 据此选策略、定下单方式与三价 |
| 是否带策略文件 | **否** | **是**（由第一次的输出决定） |
| 是否给三价 | **否**（提示词明令禁止） | **是** |
| 请求体积 | 较小 | 较大（含第一次全部内容） |

第二次默认走 **prefix chain**，把第一次的完整对话原样重发再追加，所以第二次的 `total_prompt` 能到 13 万 token。

关键代码位置：

- 装配：`pa_agent/ai/prompt_assembler.py`
- 调度：`pa_agent/orchestrator/two_stage.py`
- 缓存统计：`pa_agent/ai/deepseek_client.py`

---

## 2. 提示词文件清单

`prompt_engineering/` 共 32 个文件、**398.8KB**。装配只认 `.txt`，`_reference/` 下的 `.md` 不参与加载。

| 文件 | 大小 | 归属 |
|---|---|---|
| 市场诊断框架.txt | **85.8KB** | Stage 1 任务 |
| 二元决策.txt | 42.7KB | 两阶段共有（system） |
| 文件16-K线信号识别.txt | 20.9KB | Stage 1 + Stage 2 |
| 极速下跌分析识别.txt | 19.2KB | Stage 2 路由 |
| 极速上涨分析识别.txt | 18.8KB | Stage 2 路由 |
| 震荡区间分析识别.txt | 17.2KB | Stage 2 路由 |
| 震荡区间交易策略.txt | 16.7KB | Stage 2 路由 |
| 文件14-楔形形态分析交易.txt | 16.5KB | Stage 2 全量库 |
| 下跌通道分析识别.txt | 16.5KB | Stage 2 路由 |
| 上涨通道分析识别.txt | 15.5KB | Stage 2 路由 |
| 文件13-窄通道与宽通道策略.txt | 15.0KB | Stage 2 全量库 |
| 下跌通道交易策略.txt | 14.6KB | Stage 2 路由 |
| 文件15-二次入场机会.txt | 13.9KB | Stage 2 全量库 |
| 上涨通道交易策略.txt | 12.7KB | Stage 2 路由 |
| 极速上涨交易策略.txt | 7.5KB | Stage 2 路由 |
| 极速下跌交易策略.txt | 7.2KB | Stage 2 路由 |
| 提示词大纲_人设与思维方式.txt | 6.8KB | 两阶段共有（system） |
| 文件23-MeasuredMove与结构目标.txt | 5.8KB | Stage 2 基础四件 |
| 文件18-突破失败与突破测试.txt | 5.1KB | Stage 2 全量库 |
| 文件25-主要趋势反转MTR.txt | 4.3KB | Stage 2 全量库 |
| 文件21-铁丝网与无交易环境.txt | 4.2KB | Stage 2 全量库 |
| 文件24-最终旗形与趋势末端.txt | 4.1KB | Stage 2 全量库 |
| 文件17-止损和止盈与仓位管理.txt | 4.0KB | Stage 2 基础四件 |
| 文件20-AlwaysIn与20GB.txt | 3.7KB | Stage 2 全量库 |
| 文件19-H1H2-L1L2计数.txt | 3.3KB | Stage 2 全量库 |
| 文件27-三角形与收敛形态.txt | 3.1KB | Stage 2 全量库 |
| 文件22-信号失败后的磁力位.txt | 2.9KB | Stage 2 全量库 |
| 文件28-双重顶底与微型结构.txt | 2.4KB | Stage 2 全量库 |
| 逐棒分析检查单.txt | 2.3KB | Stage 2 基础四件 |
| _reference/pattern_enum.md | 3.3KB | **未加载** |
| _reference/abbrev_glossary.md | 1.4KB | **未加载** |
| _reference/kb_concept_map.md | 1.4KB | **未加载** |

常量定义：`prompt_assembler.py:753-824`

```python
COMMON_SYSTEM_STAGE1_TXT_FILES = (提示词大纲, 二元决策)        # 753
STAGE1_TASK_PROMPT_TXT_FILES   = (市场诊断框架, 文件16)         # 768
STAGE2_BASE_PROMPT_TXT_FILES   = (逐棒检查单, 文件16, 文件17, 文件23)  # 794
STAGE2_FULL_STRATEGY_PROMPT_TXT_FILES = (21 个策略文件)          # 801
_CHANNEL_FILE_GROUPS / _SPIKE_FILE_GROUPS = 按多空分组           # 773 / 783
```

> 注意：`文件26` 缺号；`文件23` 被归到"基础四件"而非全量库。

---

## 3. 第一次请求（Stage 1）

`build_stage1()` → `prompt_assembler.py:1048`，返回 `[system, user]`。

### system 消息（`_build_shared_system_prompt_inner`，947 行）

| # | 内容 | 大小 |
|---|---|---|
| 1 | `_LANGUAGE_ZH_RULE`（内置·中文输出规则） | — |
| 2 | `_PA_TERMINOLOGY_ZH`（内置·PA 术语中文化） | — |
| 3 | `_OPENCLAW_AGENT_NO_TOOLS_RULE`（内置·无工具） | — |
| 4 | `_THINKING_CONTENT_OUTPUT_RULE`（内置·思考输出） | — |
| 5 | `提示词大纲_人设与思维方式.txt` | 6.8KB |
| 6 | `二元决策.txt` | 42.7KB |

### user 消息（`_build_stage1_user_prompt`，1272 行）

| # | 内容 | 大小 |
|---|---|---|
| 7 | 内置文案「## 阶段一任务…（只诊断，不评估下单/止损/止盈/仓位）」 | — |
| 8 | `市场诊断框架.txt` | **85.8KB** |
| 9 | `文件16-K线信号识别.txt` | 20.9KB |
| 10 | 内置：形态识别指南 + 形态简表 | — |
| 11 | 内置：输出提醒（按 analysis_mode） | — |
| 12 | 内置：当前分析目标 + 五层窗口分层规则 | — |
| 13 | **动态**：K线数据表（100 根，含阳阴/EMA/ATR） | — |
| 14 | **动态**：K线几何特征表（14 列） | — |
| 15 | **动态**：simple_features 块 | — |
| 16 | **动态**：prefill_hint（§2.2/§2.3/§2.4 预填） | — |
| 17 | 内置：结尾提醒 | — |

### 第一次返回什么

`cycle_position`（8 态频谱定位）、`direction`、`detected_patterns`、`gate_result`、`bar_by_bar_summary`，以及 **`strategy_files_needed`**——告诉程序下一阶段该加载哪些策略文件。

---

## 4. 第二次请求（Stage 2）

`build_stage2_continuation()` → `prompt_assembler.py:1565`。

### prefix chain（默认）

```
[system]      上面 1–6（与 Stage 1 完全同一个字符串）
[user]        上面 7–17（第一次的 user，原样重复）
[assistant]   第一次返回的诊断 JSON        ← 新增
[user]        下面 18–36                   ← 新增
```

仅当 provider 不支持时降级为 standalone（`[system, user(S2 全套 + 完整 K 线表)]`）。

### 阶段二 user 消息（`_build_stage2_user_prompt`，1653 行起）

| # | 内容 | 性质 |
|---|---|---|
| 18 | 内置：API 任务规则 + 「## 阶段二任务」+ kline_intro | 静态 |
| 19 | stance_block（决策立场，由配置定） | 静态 |
| 20 | continuity_block（连续性，依赖上轮记录） | **动态** |
| 21 | conflict_block（趋势冲突，依赖本轮诊断） | **动态** |
| 22 | transition_block（过渡，依赖本轮诊断） | **动态** |
| 23 | planned_limit_block（计划限价，依赖诊断+行情） | **动态** |
| 24 | **路由来的策略文件**（1–4 个） | 半动态 |
| 25 | `逐棒分析检查单.txt` | 2.3KB |
| 26 | `文件16-K线信号识别.txt` | 20.9KB |
| 27 | `文件17-止损和止盈与仓位管理.txt` | 4.0KB |
| 28 | `文件23-MeasuredMove与结构目标.txt` | 5.8KB |
| 29 | 经验库条目（若有） | 动态 |
| 30 | `_STAGE2_OUTPUT_CONTRACT`（内置·输出契约） | 静态 |
| 31 | next-bar 指令（内置，由开关二选一） | 静态 |
| 32 | next-cycle 预测指令（内置） | 静态 |
| 33 | **动态**：阶段一诊断 JSON（紧凑版） | 动态 |
| 34 | kline_block（chain 模式下只是一句「见上方」） | — |
| 35 | prev_pred_block（上轮预测） | 动态 |
| 36 | 内置：结尾指令 + 尾部提醒 | 静态 |

### 第二次返回什么

`decision`（order_type：市价/限价/突破/不下单、entry、stop、TP1/TP2）、`decision_trace`（决策节点链）、`terminal`（最终结论）、以及交易者方程 RR 校验所需字段。

### 路由机制

```
Stage 1 输出 strategy_files_needed
    → two_stage.py:600  self._router(stage1_json)
    → 得到 strategy_files
    → stage2_user_task_txt_files() 剔除反方向的通道/尖峰文件
    → 拼到上面第 24 项
```

`stage2_user_task_txt_files()`（843 行）逻辑：

```python
routed = strategy_files
skip   = 反方向的通道文件 + 反方向的极速文件
core   = [f for f in routed if f not in skip]
core.extend(STAGE2_BASE_PROMPT_TXT_FILES)   # 基础四件固定追加在后面
return list(dict.fromkeys([*core]))          # 保序去重（无规范排序）
```

**若开启全量策略库**（`stage2_load_full_strategy_library=true`，默认 **false**，`settings.py:30`），第 24 项替换为固定顺序的 21 个文件：

```
上涨通道分析识别 → 上涨通道交易策略 → 下跌通道分析识别 → 下跌通道交易策略
→ 极速上涨分析识别 → 极速上涨交易策略 → 极速下跌分析识别 → 极速下跌交易策略
→ 震荡区间分析识别 → 震荡区间交易策略 → 文件13-窄通道与宽通道 → 文件14-楔形
→ 文件15-二次入场 → 文件18-突破失败 → 文件19-H1H2/L1L2 → 文件20-AlwaysIn与20GB
→ 文件21-铁丝网 → 文件22-磁力位 → 文件24-最终旗形 → 文件25-MTR
→ 文件27-三角形 → 文件28-双重顶底
（再接基础四件：逐棒检查单 → 文件16 → 文件17 → 文件23）
```

### 路由完整映射表

第二阶段加载哪些策略文件，**完全由第一阶段 JSON 决定**。逻辑在 `pa_agent/ai/router.py`。

#### 第一层：`cycle_position` + `direction` → 基础文件

| cycle_position | direction | 加载 |
|---|---|---|
| micro_channel / tight_channel / normal_channel / broad_channel | bullish | 上涨通道分析识别 + 上涨通道交易策略 + **文件13** |
| 同上 | bearish | 下跌通道分析识别 + 下跌通道交易策略 + **文件13** |
| 同上 | neutral | 震荡区间分析识别 + 震荡区间交易策略 + **文件13** |
| spike | bullish | 极速上涨分析识别 + 极速上涨交易策略 |
| spike | bearish | 极速下跌分析识别 + 极速下跌交易策略 |
| spike | neutral | 无 |
| trading_range / trending_tr | 任意 | 震荡区间分析识别 + 震荡区间交易策略 |
| **extreme_tr / unknown** | — | **空列表 → 不下单** |

特殊规则：

- `spike` + `spike_stage=transitioning` → 改加载通道文件（尖峰在转通道）
- `spike` + `spike_stage=ending` → 额外加载通道文件
- `micro_channel` + `spike_stage` ∈ (active, ending) → 额外加载对应方向尖峰文件
- 通道态 + neutral 时不加载方向性通道文件，改为区间策略 + 文件13（用于边界计划型限价）

#### 第二层：`detected_patterns` → 叠加文件

| 检测到 | 追加 |
|---|---|
| wedge | 文件14-楔形 |
| reversal_attempt / mtr / final_flag / h2 / l2 / 通道态 | 文件15-二次入场 |
| mtr | 文件25-MTR |
| final_flag | 文件24-最终旗形 |
| h1 / h2 / l1 / l2 / 通道态 | 文件19-H1H2/L1L2 |
| breakout_failure / failed_breakout / breakout_test / breakout_pullback | 文件18-突破失败 |
| always_in / ail / ais / 20gb / gap_bar | 文件20-AlwaysIn |
| barbwire / wire / overlap / middle_range / 区间态 | 文件21-铁丝网 |
| failed_signal / magnet / trapped_traders | 文件22-磁力位 |
| ascending / descending / symmetrical / expanding_triangle | 文件27-三角形 |
| double_top_bottom | 文件28-双重顶底 |

另外两个来源：

- `trend_context.recent_spike` 为 bullish/bearish 且 `cp ≠ spike` → 追加对应方向尖峰文件
- `alternative_cycle_position` ≠ 主状态 → 追加该备选状态的基础文件

#### 第三层：收尾

1. 去重（保留首次出现顺序）
2. **剔除反方向的通道/尖峰文件**（bearish 剔除上涨通道/极速上涨，反之亦然）
3. 末尾固定追加基础四件：逐棒分析检查单 → 文件16 → 文件17 → 文件23

#### 实例

第一阶段输出 `cycle_position=tight_channel`、`direction=bearish`、`detected_patterns=["h2","ais"]`：

```
下跌通道分析识别.txt          ← 通道态 + bearish
下跌通道交易策略.txt          ← 同上
文件13-窄通道与宽通道.txt      ← _channel_files 固定追加
文件15-二次入场机会.txt        ← h2 命中
文件19-H1H2-L1L2计数.txt      ← h2 命中
文件20-AlwaysIn与20GB.txt     ← ais 命中
── 剔除：上涨通道×2、极速上涨×2
逐棒分析检查单.txt             ← 基础四件
文件16-K线信号识别.txt
文件17-止损和止盈与仓位管理.txt
文件23-MeasuredMove与结构目标.txt
```

共 10 个文件进第二阶段。若 `cycle_position` 变成 `trading_range`，加载的会整体换成震荡区间那套。

#### 注意

- `extreme_tr` / `unknown` 返回空列表，第二阶段只剩基础四件，基本导向不下单——设计上的保险。
- 开启 `stage2_load_full_strategy_library` 会**绕过整套路由**，改为固定加载全部 21 个文件。

---

## 5. KV 缓存与 `hit`

### hit 是什么

DeepSeek **自动**上下文缓存：把请求内容从第一个 token 开始存，下次请求时逐 token 比对，**能连续对齐多少就复用多少**，这个数就是 `cached_tokens`。

- 读取位置：`deepseek_client.py:157`（`prompt_tokens_details.cached_tokens`）
- 打印位置：`deepseek_client.py:651`，日志形如
  `KV-cache: hit=73728 miss=59945 total_prompt=133673 hit_rate=55.2%`
- **命中部分约 0.1 倍价格，且跳过 prefill 计算** → 更便宜、更快
- 硬约束：**必须从开头连续相同**，中间任何一处不同，之后全部作废

### 缓存边界在哪

```
system     1–6    提示词大纲 + 二元决策        ← 固定，可缓存
user(S1)   7–12   市场诊断框架 + 文件16 + 内置   ← 固定，可缓存
────────────────────────────────────────────
user(S1)   13–16  K线数据表 / 特征表 / 预填     ← ❗每次都变，缓存到此为止
assistant         阶段一 JSON                  ← 变
user(S2)   18–36  阶段二全部内容                ← 变
```

**分叉点 = K线数据表**（`_build_stage1_user_prompt`，1316 行起拼表）。它之前全是静态，之后全部拿不到缓存。

因此 **hit 的理论上限 = K线数据之前所有静态文本的总量**。

### 实测数据（M15 实例，2026-09-24）

| 时间 | hit | miss | total | hit_rate |
|---|---|---|---|---|
| 19:04:56 | 73728 | 59945 | 133673 | 55.2% |
| 19:09:44 | 47104 | 16323 | 63427 | 74.3% |
| 19:10:45 | 62464 | 47638 | 110102 | 56.7% |
| 19:13:06 | 47104 | 16374 | 63478 | 74.2% |
| 19:14:35 | 62464 | 56583 | 119047 | 52.5% |
| 19:17:00 | 47104 | 27415 | 74519 | 63.2% |
| 19:18:00 | 73728 | 50273 | 124001 | 59.5% |
| 19:30:52 | 47104 | 28117 | 75221 | 62.6% |

单次观测：`prompt=136421 completion=15835 latency=162141ms`

**解读（推断，未最终确认）**：47104 ≈ system 那一截；62464 / 73728 ≈ system 加上部分 Stage 1 静态块。为何有时只匹配到一部分而非全部，可能与 DeepSeek 的缓存块粒度/淘汰策略有关，需抓真实请求对比确认。

### 优化杠杆

由于上限被 K 线表卡死，能动的只有：

1. **缩小"必变区"体积**（直接降 miss = 降成本降延迟）。最占地方的是 `kline_table` + `feature_table`（14 列 × 100 行）。
2. **把更多内容挪到 K 线表之前**（提高 hit 上限）。`prefill_hint` 依赖行情，挪不了。
3. **稳定第 24 项的顺序**（路由顺序由模型输出决定，不稳定）。可考虑按库内索引规范排序，或开启全量库以固定顺序换取更高命中。

> ⚠️ 曾经尝试过"把 Stage 2 内部动态块（20–23）挪到末尾"以图提升命中——**无效**，因为那段内容本就在 K 线表之后，属于永远拿不到缓存的区域。已回滚。

---

## 6. 已知问题与注意事项

1. **`文件16-K线信号识别.txt` 被加载两次**（第 9 项和第 26 项），且 Stage 2 因 chain 又重发了整个 Stage 1，所以第二次请求里它实际出现两遍。
2. **第 24 项顺序由模型输出决定**，不稳定，是前缀分叉的来源之一。
3. **`_reference/` 三个 `.md` 未参与加载**，而缩写表在 `提示词大纲` §96-109 又复制了一份 → 两份真相会漂移。
4. **同义概念多份**：`上涨/下跌通道` 与 `文件13-窄通道与宽通道` 在 Brooks 体系里本质是同一件事（通道 = 倾斜的交易区间）；极速（尖峰）又是窄通道的极端形态。全量模式下会同时加载，判定阈值若不统一易摇摆。
5. **命名与粒度不统一**：中文名与 `文件NN` 课程编号混用，文件大小从 2.3KB 到 85.8KB 差 37 倍。
6. **优先级仲裁是有效的**：`提示词大纲` 第 10-14 行的"四条全局硬禁令"显式覆盖 Brooks 原文的逆势/fade 规则，解决了多文档互相打架的问题。这个设计应保留。

---

## 7. 待办 / 可选改进

- [ ] 量化必变区各块体积（kline_table / feature_table / prefill_hint / S2 动态块），确定优化重点
- [ ] 拆分 `市场诊断框架.txt`（85.8KB），按八态拆成小文件按需加载
- [ ] 统一通道口径总纲，其余文件只引用不重复定义
- [ ] 决定 `_reference/*.md` 纳入加载还是标注为"仅供人维护"
- [ ] 评估是否开启全量策略库（固定顺序换缓存命中，但可能引入口径冲突）
